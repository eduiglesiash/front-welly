
 /*-------------------------------------------------
    #welly-frontend - #dFrontJs__example
--------------------------------------------------*/

dFrontJs.example = (function (elements) {
  'use strict';
  var elements = elements ? elements : 'empty';

  init(elements);

  function init(content){
    // console.log('content: ' + content);
    if (content === 'empty'){
      // console.log(elements);
    }
  }
});
